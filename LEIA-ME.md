# Origem Brasil

Este arquivo foi substituido pelo README principal do projeto.

Use as instrucoes atualizadas em:

```text
README.md
```

O README.md explica como instalar dependencias, importar o banco `br`, acessar o phpMyAdmin mesmo quando o Apache usa outra porta, rodar o sistema pelo XAMPP ou pelo servidor embutido do PHP e resolver problemas comuns.
